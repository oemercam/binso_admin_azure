# v18.8.1 — Header action cleanup

- Removed the global create (+) button from the application topbar component.
- Mobile/PWA creation uses only the center + in the floating pill.
- Page-level create buttons remain available on desktop but are hidden on mobile/PWA.
- Non-create page actions such as Export remain visible.
