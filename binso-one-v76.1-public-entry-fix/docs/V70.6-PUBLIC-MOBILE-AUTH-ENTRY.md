# V70.6 – Public mobile and authentication entry

- Mobile/PWA pricing uses horizontal touch scrolling with CSS scroll snap.
- Public brand shows the original Binso logo followed by ONE without a separator line.
- Mobile public navigation uses a hamburger icon.
- Footer copyright contains only Binso GmbH.
- The free-start registration entry follows the spacious public landing style.
- For Azure authentication, /sign-in now forwards directly to the configured identity provider. The redundant intermediate sign-in page is removed in production.
- Social/customer identity providers still require Azure identity configuration and are not simulated in application code.
