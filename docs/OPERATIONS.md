# Operations

Operator operations expose application-level jobs, mail status, errors, audit information and health. `/api/health/live` proves the process is live. `/api/health/ready` additionally verifies database readiness. Neither endpoint exposes secrets.
