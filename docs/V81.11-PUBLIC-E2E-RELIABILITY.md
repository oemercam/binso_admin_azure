# V81.11 Public E2E Reliability

- Public help page no longer fails when the optional help-center database is unavailable.
- Header, footer and shared page intro remain renderable in local/E2E environments without DATABASE_URL.
- Mobile landing feature sections use an explicit flex composition so copy always renders before visual content at <=820 px.
- The authenticated application remains unchanged.
