# V81.16.5 – CI version compatibility

The legacy version-gating checks no longer enumerate every patch release. They now use a shared numeric version comparison so patch releases such as `0.81.16.5` remain compatible with earlier regression checks.

This fixes the GitHub Actions failure in `v76:check` after V81.16.4 while preserving each check's minimum supported version.
