# V81.16.2 – Mobile menu lint fix

This maintenance release keeps the V81.16 canonical responsive system and V81.15 portal-based public mobile navigation while removing two React lint violations.

- Replaced effect-driven `mounted` state with hydration-safe `useSyncExternalStore` client readiness.
- Replaced pathname effect that synchronously closed state with pathname-bound open state.
- Navigation still closes on link activation, Escape, route changes, and restores body scrolling.
- No authenticated application layout changes.
