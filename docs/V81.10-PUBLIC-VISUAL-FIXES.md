# V81.10 Public Visual Fixes

V81.10 resolves the three issues exposed by the expanded V81.9 browser QA suite.

- Public subpage H1 typography now follows the landing hero scale at desktop and mobile breakpoints.
- Registration keeps the shared public header and footer mounted while session state is loading, eliminating blank-shell failures and visual jumps.
- Feature/process showcase containers no longer inherit the older negative mobile margins; screenshots are strictly contained within the viewport.
- Additional E2E assertions cover mobile title parity and registration shell stability.
- Authenticated application visuals remain unchanged.
