# V81.3 – E2E Mockup Alignment

- Keeps the V81.2 public mockup implementation unchanged visually.
- Restores the legacy `.v80-hero-visual` compatibility hook on the new V81 hero device.
- Updates responsive E2E assertions for `/sign-in` and `/admin-access` to the V81.2 mockup auth layout instead of the superseded legacy auth classes.
- Keeps mobile auth pages stacked and desktop auth pages in the two-column public mockup system.
