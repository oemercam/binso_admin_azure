# V81.15 Mobile Public Navigation

- Moves the public mobile menu panel into a `document.body` portal so sticky-header/backdrop-filter containing blocks cannot trap the fixed overlay.
- Uses explicit visible/hidden states with pointer-event protection.
- Preserves body scroll state while the menu is open.
- Adds a visible hamburger-to-close transition.
- Adds Playwright coverage for open, Escape close, route navigation and viewport containment at 390 px.
