# V81.12 Registration shell reliability

V81.12 fixes the remaining public visual-system E2E failure by ensuring the registration route always renders the shared PublicShell, including during the Suspense fallback before client hydration.

- `/register` and `/register?mode=demo` now expose header and footer immediately.
- The existing loading state remains visible inside the canonical public shell.
- No authenticated app styling is changed.
- This addresses intermittent `.v80-header` / `.v80-footer` lookup failures in the public-route visual QA loop.
