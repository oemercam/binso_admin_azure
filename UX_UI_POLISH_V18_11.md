# Binso Admin v18.11 – UI Polish

Canonical visual fixes loaded last:

- One Binso logo asset per theme; black on light, white on dark. Never both simultaneously.
- Mobile page gutter reduced to 20 px.
- Mobile navigation uses 24 px content gutters, 60 px rows, aligned chevrons, and a 44 px close target whose visible X aligns with the trailing row icons.
- Toggles have one exact 50×30 px geometry with a 24 px thumb. Legacy button and row styles cannot stretch them.
- Sheet titles/actions reuse the same mobile page gutter and aligned close affordance.
