import { ThemeControl } from '@/components/settings/theme-control'
import { PushSettings } from '@/components/pwa/push-settings'
import { getSession, signOutUrl } from '@/lib/auth/server'

export default async function SettingsPage(){ const session = await getSession(); return <section className="page"><div className="page-title"><div><p className="eyebrow">PERSÖNLICH</p><h1>Einstellungen</h1><p className="muted">Darstellung, Benachrichtigungen und Profil.</p></div></div><div className="settings-grid"><article className="panel"><h2>Darstellung</h2><p className="muted">System übernimmt automatisch Hell- oder Dunkelmodus. Eine manuelle Auswahl bleibt auf diesem Gerät gespeichert.</p><ThemeControl/></article><article className="panel"><h2>Benachrichtigungen</h2><p className="muted">Push-Benachrichtigungen können pro Gerät aktiviert werden.</p><PushSettings/></article><article className="panel"><h2>Profil</h2><dl className="summary"><div><dt>Name</dt><dd>{session?.user.name}</dd></div><div><dt>E-Mail</dt><dd>{session?.user.email}</dd></div><div><dt>Rolle</dt><dd>{session?.user.role}</dd></div></dl></article><article className="panel"><h2>Sitzung</h2><p className="muted">Auf Azure verwaltet Microsoft Entra ID die Anmeldung und SSO-Sitzung.</p><a className="button secondary" href={signOutUrl()}>Abmelden</a></article></div></section> }
