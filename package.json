{
  "name": "binso-admin-platform",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build --webpack",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "architecture:check": "node scripts/check-architecture.mjs",
    "verify": "npm run typecheck && npm run lint && npm run architecture:check && npm run test:core && npm run build",
    "build:turbo": "next build",
    "test:core": "node scripts/test-core.mjs"
  },
  "dependencies": {
    "next": "^16.3.5",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.9.3",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.3.5"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
