# Orders module

Feature boundary for orders. Keep UI, domain rules, schemas and repository-facing services for this feature inside this module. Cross-cutting infrastructure belongs in /lib; reusable generic UI belongs in /components/ui.
